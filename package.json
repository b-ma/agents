{
  "name": "agents",
  "version": "1.0.0",
  "description": "Atonomous agent library",
  "main": "index.js",
  "scripts": {
    "transform": "find . -type f | grep -v \"node_modules\" | grep .es6.js$ | while read -r line; do babel --modules common --optional selfContained $line --out-file ${line/.es6/\"\"} --source-maps-inline; done;",
    "bundle": "browserify $npm_package_main > ${npm_package_main/\".js\"/\".umd.js\"} -s $npm_package_name -d",
    "prebundle": "npm run transform",
    "watch:transform": "nodemon -e es6.js --ignore node_modules/ -x npm run transform",
    "watch:bundle": "nodemon -e es6.js --ignore node_modules/ -x npm run bundle",
    "hint": "find . -type f | grep -v \"node_modules\" | grep .es6.js$ | while read -r line; do jshint $line; done;"
  },
  "author": "Benjamin Matuszewski",
  "license": "ISC",
  "jshintConfig": {
    "esnext": true,
    "browser": true,
    "node": true,
    "devel": true
  },
  "dependencies": {
    "babel-runtime": "^4.1.1",
    "vector": "b-ma/vector2d"
  },
  "devDependencies": {
    "babel-runtime": "^4.4.3"
  }
}
